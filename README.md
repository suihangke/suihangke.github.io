# Website (Quarto)

This repository is now a **Quarto website**.

## Preview locally

```bash
quarto preview
```

## Build for GitHub Pages

This project renders into `docs/`:

```bash
quarto render
```

Then configure GitHub Pages to deploy from **branch `master` (or `main`)** and **folder `/docs`**.
